# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# rescue SSH key — never commit
ssh/

# breach evidence recovered from the hacked WordPress — never commit
_recovery/

# OpenNext/wrangler build + state outputs
.open-next/
.wrangler/

# raw team headshots (originals live outside the deployed site)
source-images/

# large recovery/extraction staging
/home/nero/Clients/gjt-extraction/

# local render/screenshot scratch
_*.mjs
docs/design-reference/BUILT-*.png

# content-generation pipeline (scripts + original-HTML references) — kept locally
_pipeline/

# session resume notes (contains SSH details)
.claude-resume.md
